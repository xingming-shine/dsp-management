# DSP 管理项目 UI 规范

## 作用范围

本规范只适用于当前仓库及其子目录，不是个人级、工作区级或跨项目 UI 规范。
不得把本文件、主题 token、组件源码或相关 AI 指令复制到其他项目。

## 技术基线

- 框架：Next.js App Router、TypeScript。
- 样式：Tailwind CSS v4。
- 组件：shadcn/ui，Radix 基座，Nova 预设。
- 主题：Neutral、CSS Variables、OKLCH 色彩。
- 参考配色：冷浅灰 `#f5f6f9` 承担系统画布与顶部栏底色，白色承担侧边
  导航、卡片、
  表格和弹层等内容面，GOFO 品牌橙 `#ff6703` 承担主操作、品牌识别、
  鼠标悬停、选中指示和键盘焦点，近黑色承担主要文字，低饱和青绿
  `oklch(0.63 0.105 178)` 承担数据强调和主图表系列。
- 图标：Lucide。

## 唯一事实来源

1. `components.json`：shadcn/ui 生成配置与路径。
2. `src/app/globals.css`：主题 token 与全局样式。
3. `src/components/ui`：当前项目拥有的 UI 基础组件源码。
4. `.agents/skills/shadcn`：当前项目的 shadcn AI 工作流。
5. 本文件：项目 UI 设计与组合约束。

## 组件规则

- 优先复用 `src/components/ui` 中的组件。
- 新建基础组件前，先通过 shadcn CLI 搜索并预览已有组件。
- 业务组件放在 `src/components` 的业务子目录，不放入 `src/components/ui`。
- 使用 `@/components/ui/...` 导入本项目组件。
- 禁止从其他仓库、其他项目目录或用户全局目录导入 UI 组件。
- 除非需求明确，不将本项目组件发布为跨项目共享包。

## 样式规则

- 优先使用 `background`、`foreground`、`primary`、`secondary`、`muted`、
  `accent`、`destructive`、`border` 等语义 token。
- 主题色、圆角、字体和深色模式只在 `src/app/globals.css` 维护。
- 浅色模式的 `background` 使用 `#f5f6f9`，`card`、`popover` 和 `sidebar`
  保持白色；边框使用 `#e4e8f0`。不在业务页面单独重复设置页面底色。
- 默认主按钮使用 `brand` 品牌橙与 `brand-foreground` 白色文字；近黑
  `primary` 保留给需要中性强强调的非按钮界面元素。
- GOFO 品牌橙通过 `brand`、`brand-hover`、`brand-selected`、`ring` 和
  `sidebar-ring` 等语义 token 使用；青绿数据强调色通过
  `data-accent` 和 `chart-1` 使用。
- 品牌橙只用于小面积识别与交互反馈：系统 Logo 细节、一级导航选中
  短指示条、组件悬停/选中态和可见焦点环。悬停使用 `brand-hover`
  的 6% 半透明橙色背景并保持深灰文字；选中使用 `brand-selected`
  浅桃色背景 `#fff4ed` 与品牌橙 `#ff6703` 的 `brand-ink`
  文字。焦点环最终可见透明度控制在
  30%–40%。深色模式下相应提高透明度，不使用大面积实色橙。不将其用于表格表头、
  常规正文或错误/警告语义；主按钮是允许使用品牌橙的唯一大面积交互控件。
- 每个页面保持不超过 2–3 个橙色视觉锚点，单个组件只保留一种橙色
  强调方式。
- 不在业务页面硬编码品牌橙或数据强调色，也不手写重复的深色模式
  颜色覆盖。
- 布局间距使用 `gap-*`，不使用 `space-x-*` 或 `space-y-*`。
- 宽高相等时使用 `size-*`。
- 条件 class 使用 `cn()`。
- `className` 主要用于布局与响应式组合，不随意覆盖基础组件的颜色和字体。

## 字体与字号

- 全系统正文、导航、表格、按钮、表单、英文、数字、页面标题、区块标题、
  核心指标、代码和编号统一使用苹方字体栈。`font-sans`、`font-heading` 与
  `font-mono` 均映射为 `"PingFang SC"`，并依次回退到冬青黑体、微软雅黑、
  系统无衬线字体。
- `font-heading` 与 `font-mono` 继续作为语义类保留，但不再切换到 Manrope 或
  Geist Mono。需要对齐的金额、时间、百分比和指标数字使用 `tabular-nums`；
  不依赖等宽字体实现列对齐。
- 正文、导航和常规表单控件默认使用 `text-sm`（14px），并继承
  `--font-weight-body`（400）常规字重；激活导航、标签、表头和需要强调的短文本
  可按组件语义使用 500。按钮统一使用 `--button-font-size`（13px）与
  `font-normal`（400）。表格内文字、辅助标签、图表刻度和低优先级说明使用
  `text-xs`（12px）。
- 强调正文和紧凑标题使用 `text-base`（16px）；区块标题按层级使用
  `text-lg`（18px）或 `text-xl`（20px）。
- 页面标题和核心指标按层级使用 `text-2xl`（24px）或 `text-3xl`（30px），
  不在业务页面使用任意像素字号。
- 移动端输入框、选择框和文本域保持至少 16px，避免移动浏览器聚焦时缩放页面。
- 字体家族只通过 `src/app/globals.css` 的主题 token 维护，不在业务组件中直接
  声明字体名称，也不通过 `next/font/google` 加载远程字体。

## 日期与时间

- 系统默认按美国常用顺序展示日期，完整日期使用
  `MM/DD/YYYY`，例如 `08/21/2026`；月份使用 `MM/YYYY`，例如
  `08/2026`。
- 日期时间统一使用 24 小时制 `MM/DD/YYYY HH:mm`，例如
  `08/21/2026 13:05`；需要秒级精度的实时时间使用 `HH:mm:ss`，
  例如 `16:42:18`。不显示 `AM` / `PM`。
- 日期范围的起止值均使用完整美国日期，中间使用 en dash：
  `08/21/2026 – 08/27/2026`。紧凑图表坐标可使用 `MM/DD`，但 Tooltip、
  表格和详情必须补全年份。
- 系统默认时区为 `America/New_York`，所有带时间的展示都必须明确
  使用当前用户时区；用户切换时区后应重新格式化同一时间点，
  不得直接修改原始时间值。
- 展示格式与机器值分离。接口、数据库、排序键和表单提交值继续使用
  ISO 8601：纯日期使用 `YYYY-MM-DD`，时间点使用含时区或偏移量的
  ISO 时间戳。原生 `input[type="date"]` 的 `value` 也保持 `YYYY-MM-DD`。
- 业务页面不得手工拼接或硬编码日期时间字符串；统一通过项目共享
  formatter 展示，基于 `Intl.DateTimeFormat("en-US")` 并显式设置
  `hourCycle: "h23"`。统一 formatter 应处理无效值、缺失值、夏令时、
  跨日与跨年场景。
- 日期时间表格列使用 `tabular-nums`；当界面上下文无法明确时区时，
  在列名、Tooltip 或区块说明中标注时区缩写。

## 时间选择控件

- 项目统一使用 `src/components/ui/period-picker.tsx` 选择业务统计周期，支持三种
  组合：单独的日/周/月单值选择、单独的日/周/月范围选择，以及通过线型 Tabs
  在单值与范围之间切换的组合选择。业务页面不得再使用原生日期输入框、Dialog 或
  自制下拉框拼装另一套时间选择交互。
- 时间选择器前方的“日/周/月”统计粒度切换统一使用项目
  `Tabs variant="raised"`，且只用于这一前置粒度入口。轨道总高度为 36px，使用
  `muted` 背景、4px 内边距与 4px 选项间距、6px 专属圆角；选项使用
  14px/500、16px 左右内边距与 4px 专属圆角。选中项使用 `card` 内容面、
  品牌橙文字和轻阴影，不绘制可见边框；Hover 使用 `brand-hover` 与品牌橙文字，
  键盘焦点使用 1px 品牌橙焦点环。
- 触发框默认宽度 320px，在窄容器内缩至 100%；高度使用默认 36px，文字为
  14px/400，使用 `tabular-nums` 与 Lucide `CalendarIcon`。触发框使用 1px
  结构边框且不添加阴影，Hover 使用 `brand-hover`，展开与键盘焦点使用项目统一的
  1px 品牌橙反馈。
- 时间面板使用 `Popover` 内容面、`rounded-lg`（6px）和浮层阴影，不绘制结构
  边框。单日、单周、单月的单值面板宽度统一为 400px，日期、周、月份范围面板宽度
  统一为 760px；两者在窄容器内均缩至 `calc(100vw - 32px)`，距视口至少 16px。
  日历内容内边距 16px，双日历或双年份间距 16px。单值模式展示一个周期面板，
  范围模式在桌面端展示两个连续周期面板；
  窄屏改为单列排列，面板内部允许纵向滚动，不得造成页面横向滚动。
- 日、周、月的可选单元桌面端统一以 32px 为基础高度；窄屏或触屏设备回到至少
  36px，并使用单列周期面板保证可点击性。日选择按周日到周六排列，单日和
  范围端点使用品牌橙圆形标记；周选择按周一到周日排列并以整周行选中；月选择按
  每行 3 个月排列。Hover 使用 6% `brand-hover`，持续范围使用
  `period-range`（浅色模式 8% 品牌橙），端点使用品牌橙实色；禁用未来周期时必须
  同时保留不可操作状态与可辨识文字，不得只依靠颜色表达选择结果。
- 组合选择顶部复用项目 `Tabs variant="line"`：高度 44px、14px/500、2px
  品牌橙指示线，文案随粒度显示“单日/日期范围”“单周/周范围”或
  “单月/月范围”。切换模式只更新草稿，只有点击“应用”才提交选择。
- 范围模式提供固定快捷项：日范围为近 7/14/31 天，周范围为近 4/8/12 周，
  月范围为近 3/6/12 个月。快捷项使用 `outline` Button，选中后使用
  `brand-selected` 与品牌橙文字，不增加阴影；范围最大值分别为 31 天、12 周和
  12 个月，超过限制时直接展示校验信息并禁用“应用”。
- 面板底部操作区最小高度为 52px，左右内边距 16px、上下内边距 8px，使用
  1px 顶部分隔线和轻量 `muted` 内容面。左侧摘要显示美国顺序的选中日期或周期及
  数量，右侧按“取消 → 应用”排列；按钮统一为 36px 高、13px/400，保持内容宽度，
  不得因面板类型改变按钮高度。
- 日期展示继续遵循项目日期时间规范：日使用 `MM/DD/YYYY`，月使用 `MM/YYYY`，
  周使用 `W{周数} · MM/DD/YYYY – MM/DD/YYYY`；接口、排序和提交值继续使用
  ISO 8601。日历周切换、方向键、Home/End、Tab、Enter/Space 与 Escape 均须可用，
  面板关闭后焦点返回触发器。

## 圆角

- 全局基础圆角 `--radius` 统一为 `0.375rem`（6px），只在
  `src/app/globals.css` 维护，业务页面不硬编码任意圆角值。
- 圆角层级沿用现有倍率：`rounded-sm` = 3.6px（0.6×）、
  `rounded-md` = 4.8px（0.8×）、`rounded-lg` = 6px（1×）、
  `rounded-xl` = 8.4px（1.4×）、`rounded-2xl` = 10.8px（1.8×）。
- 按钮、输入框、选择器、导航项和菜单项优先使用 `rounded-md`；
  表格、Popover、下拉菜单和局部容器优先使用 `rounded-lg`；Card
  和 Dialog 优先使用 `rounded-xl`。
- 嵌套容器的内层圆角不得大于外层；连接型组件只保留组合外侧圆角。
  Avatar、状态点和明确的胶囊形结构使用 `rounded-full`。

## 线条与边框

- Card、表格、表单控件、弹层和页面区域的结构边框统一使用
  `border`（1px），不使用 2px 或更粗的结构框。
- 同一视觉容器只使用一种静态层级表达：绘制结构边框时不得同时添加阴影。
  Card、表格、表单控件和描边按钮使用 1px 边框且不使用阴影；Popover、
  DropdownMenu、Select 菜单、Sheet 等浮层使用阴影时不再绘制边框或轮廓环。
  键盘焦点环与错误校验环属于短暂交互反馈，不受此限制。
- 分隔线统一为 1px，使用 `border-*`、`h-px` 或 `w-px`。相邻容器不得
  叠加两条边框；连接型组件应移除内部重复边。
- 表格外框、横向分隔线及 `variant="grid"` 的竖向分隔线均为 1px。
- Tabs 选中指示线统一为 2px，使用 `h-0.5` 或 `w-0.5`。
- 一级导航选中短指示条统一为 2px，实色使用 `brand` token，不通过
  加粗导航容器边框表达选中。
- 键盘 `focus-visible` 焦点环统一为 `ring-1`（1px），使用品牌橙
  `ring` token，最终可见透明度保持 30%–40%。错误校验状态继续使用
  `destructive` 语义，不与普通键盘焦点环混用。
- Avatar 叠放隔离环可保留 2px；Lucide 图标保留默认 2px 描边。
  这两者不属于容器结构边框。

## Tabs

- 所有业务页签使用 `src/components/ui/tabs.tsx`，保留 `default`、`line` 与
  `raised` 三套 variant；业务页面不得自行覆盖字号、高度、颜色或指示线粗细。
- Tabs 文字统一为 `text-sm`（14px）、`font-medium`，TabsTrigger 左右内边距统一为
  16px（`px-4`）。`default` 与 `line` 的横向 TabsList 高度为 44px（`h-11`）；
  `raised` 的横向 TabsList 高度为 36px（`h-9`）。
- `default` TabsList 使用基础圆角 `rounded-lg`（6px），TabsTrigger 使用
  `rounded-md`（4.8px）；`line` variant 不绘制容器圆角和背景。
- `default` variant 用于页面内部分类：TabsList 使用 `muted` 背景；选中项使用
  `brand-selected`（浅色模式 `#fff4ed`）背景与 `brand`
  （品牌橙 `#ff6703`）文字，可使用轻阴影，但不得同时绘制可见边框。
- `line` variant 用于页面主模块和看板切换：TabsList 使用透明背景与 6px
  间距（`gap-1.5`）；选中项使用 `brand`（品牌橙 `#ff6703`）文字和
  2px 指示线，不使用背景、
  可见边框或阴影。横向指示线位于底部，纵向指示线位于右侧。
- `raised` variant 仅用于时间选择器前方的“日/周/月”统计粒度切换，不用于页面
  导航、内容页签或时间面板内部的单值/范围切换。其轨道使用 `muted` 背景、4px
  内边距与 4px 间距、6px 专属圆角；选中项使用 `card` 内容面、4px 专属圆角、
  品牌橙文字与轻阴影，不绘制可见边框。两层圆角仅由 `raised` variant 维护，
  不修改或复用全局圆角倍率。
- 三套 variant 的选中文字均必须直接使用
  `brand` token，不得回退为 `foreground`、`primary` 或业务页硬编码色值。
- 三套 variant 的 Hover 均使用 `brand-hover`（浅色模式 6% 品牌橙）
  背景与 `brand`（品牌橙 `#ff6703`）文字；键盘焦点使用项目统一的
  1px 品牌橙焦点环。禁用项透明度为 50%。
- Tab 文案保持简短且不换行；窄屏优先减少 Tab 数量或允许 Tabs 区域自身横向
  滚动，不缩小字号，也不造成页面整体横向滚动。

## 弹窗与抽屉

- 短提示、确认和不超过 6 个字段的轻量编辑使用项目 `Dialog`；连续详情、多区块、
  长表单或需要保留页面上下文的任务使用项目 `Sheet`。删除及其他不可逆操作使用
  `AlertDialog` 语义，不使用浏览器原生 confirm 或业务页面自制遮罩。项目尚未安装
  对应基础组件时，先按本项目 shadcn 流程执行 `--dry-run`，再添加到
  `src/components/ui`。
- Dialog 宽度只使用四档：紧凑型 384px（`sm:max-w-sm`），用于提示、联系信息和
  单一确认；标准型 512px（`sm:max-w-lg`），用于短表单；宽型 768px
  （`sm:max-w-3xl`），用于多区块内容；最大型 896px（`sm:max-w-4xl`），用于必要
  的预览或紧凑数据内容。超过 896px、需要多列大表格或长时间操作时改用 Sheet 或
  独立页，业务页面不得设置任意弹窗宽度。
- Dialog 距视口四周至少 16px，最大高度不超过 `calc(100dvh - 32px)`；内容超出时
  只允许弹层内部滚动，不得造成页面整体滚动。Sheet 在移动端占满可用宽度，桌面端
  详情抽屉优先从右侧进入；宽度依据内容选用 384–896px，同类业务保持一致。
- Dialog 使用 `popover` 内容面、`rounded-xl`（8.4px）和统一 30px 页面边距。
  Header、Body 与 Footer 的左右内边距均为 30px；Header 顶部和 Body 底部使用
  30px。Sheet 的 Header、Body 与 Footer 沿用相同的 30px 页面边距，不得在业务
  页面单独压缩或放大。
- 标题使用 16px/500，说明与正文使用 14px/400，说明文字使用
  `muted-foreground`。弹窗内的编辑框、选择框、标签、校验和焦点状态继续复用全局
  `Input`、`Select` 与 `Field` 规范；弹窗不得另建一套表单控件字号、圆角或边框。
- 遮罩统一使用黑色 10% 与 2px 轻微背景模糊，层级由基础组件使用 `z-50` 管理。
  Dialog 使用 100ms 淡入和轻缩放，Sheet 使用 200ms 淡入和对应方向的位移动画；
  业务页面不得加入弹跳、旋转等装饰动效，并遵循系统减少动态效果设置。
- 弹层遵循“边框与阴影不叠加”：Dialog 使用 1px 轻轮廓且不添加静态阴影；Sheet
  使用阴影建立层级且不添加结构边框。内部 Header、Footer 或内容分区只使用必要的
  1px 分隔线，不得再嵌套一圈 Card 边框制造双框。
- 内容结构固定为 Header、Body、Footer。Footer 使用 1px 顶部分隔线，左右边距
  30px、上下边距 18px；桌面端与移动端操作按钮均使用内容宽度并靠右排列，空间不足
  时整体换行，不使用通栏按钮。按钮顺序为“取消/返回 → 确认/保存”，主操作位于最
  右侧，每个弹窗只保留一个品牌橙主按钮；不可逆操作使用 `destructive`。
- Dialog 与 Sheet 的 Footer 按钮统一固定为 36px 高、13px/400、`rounded-md`
  （4.8px）和 16px 左右内边距，文案不换行。所有弹层类型均复用项目 Button，
  不得因抽屉宽度、移动端布局或按钮文案长度改变按钮高度。
- 右上角关闭按钮使用项目 Button 的 `ghost`、`icon-sm` 规格和 Lucide `XIcon`，
  必须提供中文 `aria-label`。同一弹窗不得同时出现含义重复的右上角关闭按钮和
  Footer“关闭”按钮；仅在用户需要明确确认阅读结果时保留 Footer 确认按钮。
- 无未保存内容的普通 Dialog 可通过 Escape、右上角关闭按钮或点击遮罩关闭；存在
  未保存更改、进行中任务或不可逆确认时，不得因点击遮罩意外关闭，应先提示用户确认。
  提交期间禁用重复提交与关闭入口并显示 Spinner；成功后关闭并给出 toast，失败时
  保留用户输入并展示可恢复的错误信息。
- Dialog、Sheet 和 AlertDialog 必须提供可访问标题，说明文字通过对应 Description
  关联。打开后焦点进入标题后的第一个有效控件，危险确认默认聚焦安全操作；关闭后
  焦点返回原触发器。必须支持正确的 Tab 顺序、Escape 策略和可见的 1px 品牌橙
  焦点环，不得只依靠颜色或遮罩表达模态状态。

## Tooltip

### 基础 Tooltip

- 图标含义、被截断文本、快捷键和不影响决策的简短补充说明使用项目
  `Tooltip`。字段标签、校验错误、状态结果、必须处理的异常和操作确认必须直接显示
  在界面中，不得依赖 Tooltip 承载；Tooltip 内不得放置按钮、链接、输入框或其他
  需要停留操作的交互元素。
- 基础 Tooltip 使用 12px/400，正文控制在 1–3 行，默认最大宽度 280px；上下内边距
  8px、左右内边距 10px，使用 `rounded-lg`（6px）。采用高对比反色内容面，不绘制
  结构边框或静态阴影；箭头与内容面保持相同颜色。
- 默认显示延迟为 300ms，离开触发器后短延迟关闭；键盘聚焦应立即显示。Tooltip 与
  触发器默认间距 8px，优先显示在上方，空间不足时由基础组件自动翻转并限制在视口内，
  业务页面不得单独覆盖位置偏移、动画或层级。
- 鼠标悬停、键盘聚焦和触屏点击均应访问同一内容。触发器本身必须提供可访问名称，
  Tooltip 通过 `aria-describedby` 提供补充说明；关闭后不抢占焦点，也不得成为获取
  核心信息的唯一方式。

### 复杂 Tooltip

- 当用户只需查看、无需操作的结构化补充信息时，可使用复杂 Tooltip。内容允许包含
  一个 13px/500 标题、一个简短状态和 3–8 个字段；字段正文使用 12px/400。超过
  8 个字段、需要滚动、选择、复制、跳转或其他交互时，必须升级为 `Popover`、
  `HoverCard`、`Sheet` 或详情页。
- 复杂 Tooltip 默认最大宽度为 360px、内边距 12px、`rounded-lg`（6px）。使用
  `popover` 内容面和阴影建立层级，不再绘制结构边框，遵循边框与阴影不叠加规则。
  状态颜色只使用项目 success、warning、destructive 等语义 token。
- 结构化字段采用固定窄标签列与自适应值列：标签列建议 70px，列间距 10px；标签
  使用 `muted-foreground`，字段值允许自然换行，日期时间和数字使用
  `tabular-nums`。内容分区只使用必要的 1px 分隔线，不嵌套 Card。
- 复杂 Tooltip 与基础 Tooltip 使用相同的 300ms 显示延迟、8px 触发间距、碰撞翻转
  和键盘/触屏访问规则。复杂内容中的状态和说明仍为只读展示，不得借此绕过
  Popover 对交互内容的要求。

### ECharts Tooltip

- ECharts 图表 Tooltip 归入本章节的图表专用变体，并同时遵守“图表配色”章节。
  图表通过项目共享的 ECharts option/formatter 生成 Tooltip；业务图表不得各自硬编码
  背景、字号、边距、日期格式、字段间距或系列顺序。
- 多系列趋势图默认使用 `trigger: "axis"`，一次展示同一横轴位置的全部可见系列；
  单点散点、饼图和只需单个数据项的场景使用 `trigger: "item"`。Axis 模式保留 1px
  中性指示线和对应系列标记，不用品牌橙作为默认指示颜色。
- 图表 Tooltip 使用 `popover` 内容面和阴影，不绘制结构边框；使用
  `rounded-lg`（6px）、12px 内边距和 12px/400 正文。标题使用 13px/500，默认
  按图例顺序排列系列；系列色只用于 8px 标记点，字段名和值保持前景色。标记点
  必须与指标名称第一行的行盒垂直居中；指标名换行时仍对齐第一行，不得相对整行
  内容居中，也不使用固定 `margin-top` 模拟居中。
- 图表 Tooltip 外层最大宽度为 360px，左右内边距均为 12px；内容区使用
  `fit-content` 随内容自然收缩，不设置固定宽度或通用最小字段宽度。默认采用“8px 系列
  标记 / 自适应指标名 / 右对齐指标值 / 可选状态”四列共享网格。指标名与对应值之间固定
  保留 38px 间距，状态列出现时宽 72px；不得通过 `space-between` 或弹性占满剩余宽度
  制造空白。无状态、汇总或扩展内容的简单 Tooltip 应保持紧凑；复杂内容才扩展，超过
  最大宽度后仅指标名换行。值列中的数字统一右对齐并使用 `tabular-nums`，单位紧跟数值；
  同一 Tooltip 的所有指标值纵向对齐，所有状态标签也纵向对齐，行高至少 24px。
- 长指标名允许在指标名列内自然换行，不挤压或错位值列。任意一行出现状态时，全部行
  必须保留状态列；完全没有状态时才折叠状态列。状态采用项目 `sm` 标签的 20px 高度、
  4px 圆角和 success、warning、destructive 等语义色，不使用品牌橙表达业务状态。
- 汇总数据位于必要的 1px 分隔线下，并复用相同的指标名和值列，不另起一套对齐基线。
  原始值、差值、口径说明等次级信息放入对应行的扩展说明区域；需要增加来源、更新时间
  或其他只读内容时，继续使用标题上下文、行说明、汇总或底部备注插槽，不破坏四列结构。
- 标题必须展示当前横轴值。日期使用美国顺序，日期时间使用项目统一的 24 小时制，
  例如 `08/21/2026 14:00`；字段名、数值和单位不得省略。数值 0 必须显示为 0，
  缺失值显示 `—`，不得出现 `undefined`、`null` 或误把空值格式化为 0。
- Tooltip 应保持在图表容器内，靠近数据位置但不得遮挡当前标记、坐标轴标题或图例；
  触屏点击后允许固定，再次点击空白处关闭。图表 Tooltip 不承载操作按钮，若需要
  钻取或编辑，应提供独立、可聚焦的图表操作入口。
- 图表信息不得只依赖鼠标悬停。图表需要可访问名称和摘要；关键指标应通过直接标签、
  图例、数据表或详情视图提供键盘与辅助技术可访问的等价信息。

## 按钮

- 所有业务操作统一使用 `src/components/ui/button.tsx`，保留 `default`、
  `outline`、`secondary`、`ghost`、`destructive` 和 `link` 六种 variant；
  业务页面不得覆盖按钮字号、字重、颜色或交互状态。
- 按钮基线字号统一为 `--button-font-size`（13px），字重为 `font-normal`（400），
  基础圆角使用 `rounded-md`（4.8px）。默认尺寸高 36px、左右内边距 16px；
  `xs`、`sm`、`lg` 高度分别为 24px、32px、40px。
- `default` 是主按钮，使用品牌橙 `brand`（`#ff6703`）背景与白色
  `brand-foreground` 文字。每个页面、Dialog 或独立操作区只保留一个主按钮。
- `outline` 使用 1px `border` 和 `card` 背景且不使用阴影；`secondary` 用于
  次级填充操作；`ghost` 用于低强调操作；`destructive` 仅用于删除或不可逆操作。
- `link` 是文本按钮，默认使用品牌橙文字；Hover 时增加 `brand-hover` 底色框，
  不通过下划线表达悬停。
- 纯图标按钮（Icon-only Button）使用同一 `Button` 组件的 `icon-xs`、
  `icon-sm`、`icon` 和 `icon-lg` 尺寸，可视尺寸分别为 24px、28px、36px 和
  40px。`icon-xs` / `icon-sm` 仅用于紧凑表格和工具栏；独立操作优先使用
  `icon`，更强调的操作使用 `icon-lg`。
- 普通纯图标按钮统一使用 1px `border`、`card` 背景和品牌橙图标，
  不使用阴影；Hover 使用 `brand-hover`，持续选中状态使用
  `brand-selected` 并设置 `aria-pressed`。危险图标按钮继续使用
  `destructive` 语义。Lucide 图标自身保持默认 2px 描边，1px 仅指按钮外框。
- 纯图标按钮必须提供明确的 `aria-label`；含义不熟悉或需要补充说明时组合
  项目 `Tooltip`。不得仅依赖图标形状表达删除、提交等高风险操作；
  加载时用 `Spinner` 替换图标并同时设置 `disabled`。
- 普通按钮 Active 时向下位移 1px，Disabled 透明度为 50%；键盘焦点使用项目统一
  的 1px 品牌橙焦点环。按钮内图标使用 `data-icon="inline-start"` 或
  `data-icon="inline-end"`，不在业务页面单独设置图标尺寸。

## 标签

- 状态、分类、数值标记和查询条件中的可移除标签统一基于
  `src/components/ui/badge.tsx`；业务页面不得使用自制 `span` 重复实现标签的
  高度、字号、内边距、圆角、颜色或交互状态。标签尺寸与 variant 表达不同维度：
  `size` 只控制几何和字体，`variant` 只控制业务语义与外观。
- 标签提供 `sm`、`md`、`lg` 三档尺寸，圆角均固定为 4px，不跟随全局 6px
  基础圆角倍率变化：

  - `sm`：高 20px、12px/400、左右内边距 6px，用于表格、密集列表和紧凑数值标记。
  - `md`：高 24px、12px/500、左右内边距 8px，用于 Card、详情和常规状态/分类。
  - `lg`：高 28px、13px/500、左右内边距 10px，用于摘要区、详情标题旁和需要重点
    识别的业务提示；表格、密集列表和查询条件内不得使用 `lg`。

- 标签内部图文间距统一为 4px；`sm`、`md` 图标使用 12px，`lg` 图标使用 14px，
  Lucide 图标保留默认 2px 描边。只有图标能帮助快速区分状态或含义时才添加，
  不为所有标签装饰性地配置图标。
- 待处理和普通分类使用中性 `muted` 填充或 1px `border` 轻描边；处理中使用信息
  语义，成功、提醒和异常分别使用 `success`、`warning`、`destructive` 语义。
  业务状态不得使用品牌橙冒充警告或异常色。填充型标签使用透明边框，描边型标签
  使用 1px 边框且不添加阴影，遵循边框与阴影不叠加规则。
- 标签默认是只读信息，不因鼠标移入产生可点击暗示。确需选择、移除或切换时，
  使用具备对应语义的可聚焦控件组合：Hover 使用 `brand-hover`，持续选中使用
  `brand-selected` 与品牌橙文字，键盘焦点使用 1px 品牌橙焦点环；禁用状态透明度
  为 50%，且不得仅依赖颜色表达状态。
- 查询条件中的可移除标签属于独立的筛选标签形态：高 28px、14px/400、左右
  内边距 8px、4px 圆角，使用中性 `muted` 底色、无边框和品牌橙移除图标。
  其 28px 高度不等同于 `lg` 强调标签，不继承 `lg` 的字号、字重或使用场景。
- 标签内容保持单行，建议不超过 12 个汉字；超出可用宽度时省略并通过项目
  `Tooltip` 提供完整内容。数值与单位紧跟标签文字并使用 `tabular-nums`，0 必须
  显示为 0，缺失值显示 `—`。同一组标签使用 6px 间距，并允许自然换行，
  不得通过缩小字号塞入单行。
- 表格中的状态与分类统一使用 `sm`，并跟随所在列左对齐；Card 和详情默认使用
  `md`。同一业务状态在不同页面必须保持 variant 与颜色身份一致，不因页面布局
  临时换色。可移除标签的移除按钮必须提供明确的 `aria-label`，动态移除后应向
  辅助技术传达剩余已选数量。

## 图表

### 排名差距分析

- 数据驾舱的排名差距提示固定使用“总分差距 → 模块差值 → 建议”三行信息结构，
  不得将结果、原因和行动建议合并为连续长段落，也不额外显示“分数差距”“模块差值”
  或“建议”等行标题。三行内容左对齐；行间距为 8px，容器内边距为 16px。
- 站点排名标题只显示文字，不在两侧添加麦穗等装饰图标。排名差距提示的前导图标
  与第一行摘要的文字行盒垂直居中，不得相对整个三行内容区域居中。
- 第一行使用 13px 展示与当前对比第一名的总分差和对应排名；总分差使用品牌橙强调。第二行
  展示全部模块差值并按差距降序排列，单个标签不换行，标签组可自然换行。第三行
  只保留一条由主要差距生成的综合建议，使用 12px 辅助文字，桌面端以两行内为目标，
  不重复陈述差距数值。
- “站点第一名 / 大区第一名”是互斥的对比口径，使用项目
  `ToggleGroup variant="raised"` 而不是导航 Tabs。该变体沿用时间粒度切换的视觉语言：
  36px 高的 `muted` 轨道、4px 内边距与间距、白色选中内容面、轻阴影和品牌橙文字，
  不绘制结构边框。桌面端位于第一行右上角，窄屏移到三行内容上方。同一个选中值必须
  统一驱动总分差、排名、模块差值、建议和关联雷达图；雷达图只保留当前 DSP 与已选第一名。

### 配色

- 图表统一使用 `src/features/data-cockpit/components/echarts-chart.tsx` 读取
  `src/app/globals.css` 中的语义 token；业务页面不得直接写入 Hex、RGB 或
  Tailwind 色值。浅色和深色模式保持相同的系列色身份。
- 标准系列色按以下固定顺序使用：`chart-1` 青绿 `#5bc49f`、`chart-2`
  蓝 `#60acfc`、`chart-3` 青 `#32d3eb`、`chart-4` 琥珀 `#feb64d`、
  `chart-5` 紫 `#9287e7`、`chart-6` 珊瑚 `#ff7c7c`。系列颜色跟随图例
  顺序依次分配，不因筛选、排序或页面切换改变同一指标的颜色身份。
- 单系列和主分析系列优先使用 `chart-1`；多系列优先使用前 6 个标准色。超过
  6 个系列时应优先合并低价值分类；确需扩展时，只能从标准色叠加约 20% 白或
  10% 黑形成明度层级，并在全局 token 中登记，不在业务页面临时生成杂色。
- 业务语义优先于系列顺序：成功使用 `success`，提醒使用 `warning`，错误、失败
  和高风险使用 `destructive`，待处理或未开始使用 `border`/`muted`。珊瑚色
  `chart-6` 仅用于无正负含义的分类；存在异常语义时必须改用语义色。
- 折线图在系列较少时使用克制的标准色，系列较多时提高色相区分但不得使用高饱和
  撞色；面积填充使用对应系列色约 10% 不透明度，叠加系列不超过 4 个。
- 柱状图按主题系列色区分类别；同一指标跨时间比较保持同色。正负、达标/未达标
  等状态不得只依靠系列色，必须同时使用标签、图例或形状说明。
- 饼图用于有明确整体关系的数据。连续或有序分段优先采用同一色相的明度层级；
  无序类别使用标准系列色。类别过多时合并为“其他”，避免依靠大量近似颜色区分。
- 渐变只用于连续数值、密度或强调范围，且必须使用同一色系、控制明度跨度；禁止
  装饰性彩虹渐变。图表背景保持透明，由白色 Card 承载；坐标轴与网格线使用
  `border`，图例与辅助文字使用 `muted-foreground`。
- GOFO 品牌橙 `brand` 不进入默认图表系列色板，只用于图表筛选的 Hover、选中、
  键盘焦点以及确有品牌含义的单个标记；同一图表不得把品牌橙同时用作系列色和
  交互强调色。
- 颜色不得成为唯一的信息载体；状态、阈值和异常必须辅以文字、数值、线型、形状
  或图例。图表文字与背景保持清晰对比，避免高彩度颜色作为大面积背景。

### 线条、节点与柱体

- 常规折线宽度统一为 2px；网格线、坐标轴线和 Tooltip 轴指示线统一为 1px。
  业务页面不得通过加粗折线表达选中或告警，选中和异常应使用语义色、标记点、标签
  或辅助线表达。
- 折线常态节点统一为直径 6px 的圆点，Hover / Emphasis 节点放大为 10px；同一图表
  不得按系列任意设置不同节点尺寸。高密度连续数据可隐藏常态节点，但 Hover 节点仍
  保持 10px，并须保留 Tooltip 和可访问的数据替代视图。
- 柱状图单柱最大宽度为 26px，默认使用 `barMaxWidth: 26`；类别沿 Y 轴排列的
  紧凑横向排行图可使用 `barMaxWidth: 20`，但不得超过 26px。这里的 20px 不适用于
  日期沿 X 轴排列的密集分组时间序列。纵向柱体顶部圆角统一使用 4px、底部保持直角；
  堆叠柱只有当前可见堆叠的最上层柱段绘制 4px 顶部圆角，内部柱段保持直角。横向排行柱
  末端继续使用 2px 圆角。分组柱之间应保留清晰间距，不得通过放大柱宽填满整个类目槽位。
- 一屏展示 31 天、每天 2 根柱的密集分组时间序列统一使用
  `barMaxWidth: 12`、`barGap: "20%"` 和 `barCategoryGap: "30%"–"40%"`。
  约 1200px 宽的图表容器内，单柱实际宽度应控制在 10–12px；容器低于 1000px 时
  允许由 ECharts 自动收窄到 8–10px。必须设置最大宽度而非固定 `barWidth`，避免
  62 根柱体及组间距溢出绘图区。
- 密集时间序列不得通过隐藏任一系列或压缩图例来强行适配。当单柱宽度低于 8px、
  日期刻度重叠或数据项已无法可靠 Hover 时，应启用横向 `dataZoom` 或切换时间范围，
  不再要求全部数据同时显示在一屏；滚动行为和样式统一遵循“横轴标签与滚动”。
- 面积图继续使用对应系列色约 10% 不透明度；折线、节点、柱体的 Hover 只增强当前
  数据项，不改变同一指标的系列色身份，也不使用品牌橙覆盖分析色。

### 图例与坐标区

- 趋势图、柱状图和多系列对比图的图例默认水平居中置顶；饼图、环图和雷达图的图例
  默认水平居中置底。空间不足时使用 ECharts `legend.type: "scroll"`，不得缩小到
  12px 以下，也不得让图例覆盖绘图区。
- 常规图例图形宽 14px、高 8px，图例项间距 16px；紧凑分布图可使用宽 10px、
  高 8px、间距 12px。图例文字统一为 12px/400，使用 `muted-foreground`；图例
  顺序与系列色顺序保持一致。
- 图表 Card 的绘图区内容左右内边距统一为 16px。标准单轴直角坐标系使用
  `grid: { top: 60, right: 16, bottom: 56, left: 16, containLabel: true }`；双 Y 轴图左右
  网格边距均为 24px。横向排行图左右网格边距也使用 16px。依靠 `containLabel: true`
  容纳必要的轴标签，不再叠加不对称的大额预留；只有在真实数据验证仍会截断时才能成对
  增加左右边距，业务页面不得为留白随意缩小绘图区。启用横向 `dataZoom` 时底部边距
  使用 76px。离散对象同时使用倾斜标签和 `dataZoom` 时，标签高度仅由
  `containLabel` 计算，不得在底部边距中重复预留；底部边距仅包含距底部 12px、
  高 8px 的滚动条和 16px 标签安全间距，当前标准值为 36px。
- 坐标轴刻度文字与轴线间距统一为 8px，坐标轴名称与轴主体间距统一为 12px。
  坐标轴、刻度和网格线使用中性语义 token，刻度文字为 12px/400；日期轴使用
  `MM/DD` 紧凑格式，完整日期和时间由 Tooltip 按项目日期时间规范展示。

#### 横轴标签与滚动

- 日期、时间和连续周期等规则序列允许按固定间隔省略横轴刻度，但不得省略数据点；
  默认保持水平显示，不因数据密集自动倾斜。完整日期、时间和值由 Tooltip 与明细表提供。
- 司机、路区、站点等离散对象在当前可视窗口内必须逐项生成标签，不得自动隐藏。
  标签统一向右倾斜 75°，使用 12px/400，轴上展示宽度不超过 96px；标签宽度以完整
  数据的第 90 百分位为基准，超出时显示省略号，Tooltip 与明细表展示完整名称。
  “逐项显示”指每个可见对象都有标签，不要求每个长名称在坐标轴上完整展开。
- 离散对象的总数只决定是否需要横向滚动，不直接决定同屏数量、柱宽或柱组间距。
  最小类目槽位取“75° 标签的实际横向投影宽度”和“柱组保持单柱至少 8px 所需宽度”
  的较大值；同屏数量使用绘图区宽度除以最小类目槽位并向下取整，在满足上述可读性
  约束的前提下展示尽可能多的对象。堆叠系列按一个柱组计算，折线系列不计入柱组宽度。
- 离散对象柱图根据当前可视数量连续调整间距，不根据完整数据总数套用密集时间序列
  参数：1–5 个对象时柱组间距为 40%–48%、组内柱间距为 18%–20%；6–10 个时分别为
  28%–36% 和 14%–18%；11–18 个时分别为 18%–28% 和 12%–16%；19 个以上时分别为
  16%–20% 和 10%–12%。单柱宽度保持 8–26px，不设置固定 `barWidth`；同屏数量计算
  必须同步使用 16% 的柱组间距下限和 10% 的组内柱间距下限，空间不足时
  应减少同屏对象数，不得继续压缩间距。
- 离散对象超出绘图区容量时启用横向 `dataZoom`，保持窗口宽度固定，只允许平移；
  鼠标、触控板和触屏滚动不得触发缩放。滚动条统一使用 8px 高的中性轨道与单一滑块，
  轨道和滑块的可见填充均使用 4px 圆角，不显示数据缩略图、数值详情、两侧缩放手柄
  或中央拖拽把手。轨道与滑块使用 `muted` / `muted-foreground` 语义色，不使用
  品牌橙或图表系列色。
- 横坐标继续按选中周期与现有时间窗口展示，不得为了制造波动裁切、平移或补入虚假
  类目。变化趋势主要通过纵坐标范围表达，并由项目共享配置统一计算。
- 纯折线和面积趋势图的数值纵轴使用局部刻度：不强制从 0 开始，以全部可见系列、
  目标线和有效数据的最小值、最大值为基准，上下各增加 12% 留白，再按 1、2、5 的
  易读步长取整，默认划分为 5 段。数据全部相同时仍须展开最小可视范围，禁止出现
  上下限相等；非负指标不得因留白产生负刻度。轴名称应附加“局部刻度”提示，双轴或
  窄屏可将单位与提示分为两行，并向绘图区内侧对齐，禁止贴边截断。
- 百分比折线的局部刻度限制在 0%–100%，可视范围不得小于 5 个百分点；数值 0、
  缺失值和未完成周期继续按项目 Tooltip 与数据状态规则处理，不得通过归一化值替代
  真实值。多条同单位折线必须共用同一纵轴范围，不能分别缩放。
- 纵向柱状图、堆叠柱状图以及任一同单位轴中包含柱体的组合图，数值纵轴必须从 0
  开始，禁止使用局部刻度放大柱长差异。纯负值柱体以 0 为上限；同时包含正负值的
  偏差图以 0 为中心，按最大绝对值加 12% 留白后对称展开。横向柱状图的数值轴遵循
  相同基线规则，即使该数值轴位于 X 轴。
- 双 Y 轴和柱线组合图按轴独立判断：包含柱体的轴从 0 开始，纯折线轴使用局部刻度；
  每个轴必须标明单位，纯折线轴同时标明“局部刻度”。同一单位不得为了突出趋势拆成
  两个不同范围的轴，Tooltip 始终展示原始真实值。
- 实时或轮询更新的趋势图在观察窗口、筛选条件和系列集合不变时应保持当前纵轴范围；
  只有新数据超出范围才按同一算法向外扩展。切换时间范围、筛选条件或系列后才重新
  收紧范围，避免纵轴随每次刷新抖动并制造虚假的趋势变化。
- 图表 Card 标题、工具栏和说明不计入 ECharts 绘图区高度；图例、坐标轴与网格必须
  在下列绘图区高度内完整显示。禁止通过负边距或绝对定位把图例、刻度移出图表容器。

### 推荐高度

- 图表绘图区只使用四档推荐高度，业务页面不得设置任意高度：

  - 紧凑型 224px：小型饼图、环图、雷达图和单指标分布。
  - 标准型 288px：常规 Card 内的单轴折线图、柱状图和组合图，也是默认高度。
  - 主分析型 360px：页面主图、多系列趋势和需要较长横轴标签的分析图。
  - 详情型 400px：双轴组合图、密集标签或独立详情分析区。

- 同一行的可比较图表使用相同绘图区高度。窄屏不把图表压缩到 224px 以下；优先
  调整布局、滚动图例或改为单列，并保持坐标、图例和 Tooltip 可读。
- 项目共享 ECharts 容器默认使用 288px；只有图表语义符合上述紧凑、主分析或详情
  场景时才显式切换档位。高度档位、图例位置、坐标区边距、折线、节点和柱宽应由共享
  图表配置统一提供，业务页面不得重复硬编码。

## 表格

- 所有业务表格使用 `src/components/ui/table.tsx` 提供的项目基础组件，不在页面
  重复实现表格边框、字号、行高和交互状态。
- 表格内文字统一使用 `text-xs`（12px），表头使用 `font-medium`（500），普通
  单元格继承正文常规字重（400）。
- 表头和数据行统一采用宽松的 48px 行高；表头使用中性浅色背景。
- 表头与表体所在的表格区域必须具有完整的 1px 外边框，使用语义 `border` 和项目
  `rounded-lg` 圆角，不添加阴影。外框包围表格的可视区域，表格内部滚动时仍保持
  完整可见；不得仅用查询区、表格、分页共同的数据区域外框替代。
  若数据区域也保留外框，两层外框须通过内边距隔开，避免相邻边框重叠。
- 所有表格保留轻量横向分隔线，不默认使用斑马纹；悬停使用
  `brand-hover`，选中使用 `brand-selected`。
- 字段较少、以阅读和操作为主的列表使用默认 `<Table>`，不添加竖向分隔线。
- 字段较多、需要横向比较连续指标的数据表使用 `<Table variant="grid">`，在列间
  添加竖向分隔线。是否使用 `grid` 由业务语义决定，不根据数据行数量自动切换。
- 同一列的表头与单元格必须使用相同的水平对齐方式。普通文本、编号与编码、状态与
  分类、数量与计数、金额与比率、日期与时间等业务字段统一左对齐；运单编号、任务
  编号和邮编属于识别码，不按数值列处理。状态使用项目 `Badge`。
- 数量、金额、百分比和日期时间仍使用 `tabular-nums`，同列保持一致的小数位数；
  单位紧跟数值，不拆成两端对齐。`—`、0 和其他空值占位符沿用所在列的左对齐，
  不单独居中。可排序字段的按钮与图标跟随字段左对齐，排序图标紧跟字段名。
- Checkbox、布尔值和纯图标列居中；操作列表头及按钮组整体居中。默认单元格垂直居中；明确允许换行的
  备注或说明字段顶部对齐，并保持至少 48px 行高。表头与单元格统一使用 12px 水平
  内边距，确保同列文字起始线一致。
- 操作列存在多按钮组合时，各行使用统一宽度的按钮组，在单元格内整体居中；组内
  按钮左对齐、间距统一。同一表格各行的首个操作按钮保持纵向对齐，即使某行仅有
  一个按钮，也沿用相同的按钮组宽度，不随按钮数量变化而移动。
  按钮组宽度根据本列最长操作组合及可用空间确定，不设置项目统一的固定像素宽度。
- 表头保持简短且不换行。行操作位于最右侧，优先使用项目按钮或
  `DropdownMenu`，不得压过主要数据。凡存在行操作的表格，操作列必须固定在表格
  可视区域最右侧；表头操作列与每一行操作单元格同步固定，横向滚动时保持可见。
- 固定操作列使用不透明的 `card` 内容面，并以左侧 1px 分隔线与数据列区分，不添加
  阴影。行 Hover 或选中时，操作单元格背景必须同步使用该行的 `brand-hover` 或
  `brand-selected`，避免固定列出现断层；操作列宽度应计入表格布局，不得覆盖末列
  数据。固定定位、层级和背景由项目基础表格统一处理，业务页面不得各自实现。
- 固定列验收：横向滚动至起点、中间、末尾时，左侧 1px 分隔线均须可见，表头与
  数据行连续对齐；默认、Hover 和选中状态下不得被单元格底色或滚动内容遮挡。
- 表格在窄屏允许自身横向滚动，不得造成页面整体横向滚动，也不得通过缩小字号
  容纳更多字段；移动端优先保留关键列，将次要信息移入详情面板或详情页。
- 筛选、批量操作、导出、记录数和分页应与表格组成一个连续的数据区域。
  加载、空结果和失败状态分别使用项目 `Skeleton`、`Empty` 和 `Alert`/toast。

## 多选与单选

- 平铺多选使用 `src/components/ui/checkbox.tsx`，查询条件中的下拉多选使用
  项目 `DropdownMenuCheckboxItem` 组合，互斥选项使用项目 `RadioGroup`；
  业务页面不得手写选择框的尺寸、描边、勾选图形、颜色或焦点状态。
- 彼此独立且可同时成立的选项使用多选；同一时刻只能保留一个值的选项使用单选。
  不得用多个 Checkbox 模拟互斥选择，也不得用 RadioGroup 表达可多选条件。
- 当一个多选组有 3 项及以上、且“全选”对用户有明确意义时，首项提供“全选”
  Checkbox。全选项与其他选项之间使用 1px 分隔线；其文字使用常规 14px/400，
  选中数量说明使用 12px/400，不以加粗或大面积品牌色制造额外层级。
- 父级 Checkbox 必须同步反映子项状态：全部选中显示勾选；部分选中显示短横线
  （`indeterminate`）；未选显示空框。点击未选或半选状态时选中全部子项，点击
  全选状态时清空全部子项；直接改变任一子项后立即重新计算父级状态和已选数量。
- 选择框 Hover 使用 `brand-hover` 与品牌橙描边反馈，持续选中使用 `brand`；
  键盘操作使用项目统一的 1px 品牌橙焦点环。选择状态不得只依赖颜色表达，
  必须保留勾选或半选图形及可访问名称。
- 表格的行选择使用表头 Checkbox 作为当前页的快捷全选入口。表头全选、半选、
  清空都只作用于当前可见页记录；不提供跨页或“全选全部查询结果”的入口、提示条
  或批量选择状态。翻页后按新页面的行选择状态重新计算表头状态，批量操作仅处理
  当前页已选记录。
- 每个 Checkbox 和 RadioGroup 均应有可访问名称；表头选择框的名称明确为
  “全选当前页记录”。禁用项保持可辨识的文字与状态，不得仅降低对比度；所有选择
  操作必须支持键盘切换并在变化后向辅助技术传达选中数量或状态。
- 查询条件的下拉多选与所在查询字段同宽，高度沿用默认 36px，使用
  项目统一的 `ChevronDown` 箭头。多选与单选 `Select` 的展开箭头均为
  `size-4`（16px），颜色均为 `muted-foreground`，由基础触发器统一控制尺寸；
  业务页面不得单独放大、缩小或换用其他展开图标。已选值直接放在选择框内，标签使用中性
  `muted` 底色、无边框、4px 圆角、14px/400 文字，移除图标使用
  品牌橙。每个已选标签必须可通过点击或键盘移除，并提供明确的
  `aria-label`；容器空间不足时保留前两项并以 `+N` 表示其余项。

## 表格查询工具栏

- 需要查询条件的运营列表统一采用“查询工具栏 + 表格 + 分页”的单一数据区域。
  查询条件、导出、批量操作、记录数和分页保留在同一内容面，不得拆成彼此独立的
  Card。表头与表体所在的表格区域按“表格”章节绘制独立、完整的 1px 外框，
  并与查询工具栏的内容边距对齐。数据区域可保留外框，两层外框通过内边距隔开；
  内部分隔线仅在必要处使用 1px，不使用静态阴影。
- 查询字段使用 `FieldGroup`、`Field` 与项目 `Input`、`Select`、日期控件等基础
  组件组合；业务页面不得手写控件边框、字号、焦点或状态颜色。查询字段的可见标签
  使用 `text-sm`（14px）/500，且不添加左内边距，标题文字起始线与查询控件外边框
  对齐；控件文字使用 `text-sm`（14px）/400，控件高度使用默认 36px，字段间距为
  12px，工具栏内边距为 16px。仅供辅助技术读取的 `sr-only` 标签保持隐藏状态。
- 桌面端查询条件每行最多放置 4 个，按等宽四列排列。关键词、状态、日期/
  日期范围与路区等为优先条件；低频或组合条件可折叠到“更多条件”，展开后按
  四列继续换行，不能挤压首行高频条件。
- 当查询条件少于 4 个时，“查询 → 重置”使用剩余的最右列，与查询条件保持
  同一行并靠右；当查询条件达到或超过 4 个时，查询与重置另起一行并靠右。
  导出及“更多条件”等辅助操作只在独立操作行出现，并放在左侧，与右侧查询、
  重置形成两端对齐。查询使用唯一的 `default` 主按钮，重置与导出使用
  `outline`；不为查询工具栏添加第二个实色主按钮。导出必须基于当前已提交的
  查询条件。
- 不在查询工具栏下方重复展示“已选条件”行。多选值仅在对应选择框内
  以可移除标签展示；其他已填条件保留在原字段中，避免形成两套状态。
- 点击查询后刷新表格并回到第 1 页；重置应清空全部条件、字段内已选值和展开状态，
  并回到第 1 页。查询、重置、导出及移除标签均应提供可见的键盘焦点状态；加载、
  空结果和失败状态分别使用项目 `Skeleton`、`Empty` 和 `Alert`/toast。
- 窄屏及固定窄栏下查询字段按单列排列，不强制套用桌面端同行规则；查询和重置
  统一在字段后换行并靠右，顺序保持不变。存在独立操作行时，导出保持在左侧组，
  查询和重置保持在右侧组。不得隐藏必填或高频条件，也不得造成页面整体横向滚动；
  字段较多的表格只允许表格区域自身横向滚动。

## 列表排序交互

- 列表的独立排序入口优先放在查询工具栏独立操作行左侧，与右侧的
  “查询 → 重置”同一行；当少于 4 个查询条件使查询、重置与字段同行时，排序入口
  可保留在结果工具行，不得为了与查询按钮同行而挤压查询字段。不使用带边框的
  Select，不在排序入口旁重复展示记录总数。记录数是否展示由页面与分页规范决定，
  不因排序入口而额外增加。
- 复用项目 `Button`（`variant="ghost"`、`size="sm"`）和 `DropdownMenu`。
  入口采用“图标在左、当前排序方式文字在右”，使用 Lucide `ArrowDownUpIcon`
  上下双箭头，不使用长短横线或筛选漏斗图标。图标设置 `data-icon="inline-start"`，
  尺寸、字号、字重和间距沿用基础组件，不绘制静态边框或阴影。
- 默认入口文字为“默认排序”，图标与文字保持中性色。选择非默认项后，入口文字
  自动替换为所选排序方式，只有图标使用 `text-brand` 变为品牌橙；常态文字不变橙，
  不增加持续填充背景或边框。悬停、菜单展开和键盘焦点仍沿用基础组件交互反馈。
  切回“默认排序”或点击“重置”时，恢复默认文字与中性图标。
- 下拉菜单采用单选，使用 `DropdownMenuGroup` 内的 `DropdownMenuRadioGroup`
  与 `DropdownMenuRadioItem`，以勾选标记指示当前项；菜单左对齐入口，宽度应能
  完整展示选项，不随入口短文案压缩。
- 司机监控地图的选项与顺序固定为：“默认排序”“超期天数从高到低”
  “超期天数从低到高”“妥投时间升序”“妥投时间降序”。默认按派送顺序；
  时间升序为从早到晚，降序为从晚到早。其他业务列表沿用交互模式，按业务定义
  排序字段，不直接套用司机监控的字段。
- 选项选择后立即生效并关闭菜单，无需再次点击“查询”；对当前已提交的筛选结果
  整体排序，再分页，回到第 1 页并将列表滚动到顶部。排序不提交尚未查询的筛选草稿。
- 入口使用 `type="button"`，避免触发表单查询；可访问名称包含排序用途与当前值，
  例如“运单排序：超期天数从高到低”。支持键盘打开、方向键导航、确认选择、
  Escape 关闭及焦点返回入口，保留可见焦点环，不仅依赖颜色传达当前排序。
- 窄屏保留完整排序文字与查询、重置操作；空间不足时允许操作行换行，不缩小字号，
  不造成页面横向溢出。

## 分页

- 数据列表统一使用 `src/components/ui/pagination.tsx`。基础页码可组合
  `Pagination` 系列原语；完整数据表优先使用受控 `DataPagination`，不得在业务页面
  重复实现页码计算、跳页校验或分页交互样式。
- 分页字号统一为 `--pagination-font-size`（13px）、字重为 400；当前页使用 500。
  控件高度统一为 `--pagination-control-height`（32px），页码间距使用
  `--pagination-gap`（6px），基础圆角使用 `rounded-md`（4.8px）。
- 页码、上一页、下一页、页容量选择和跳页输入使用 1px 结构边框且不使用阴影。
  Hover 使用 `brand-hover` 背景与品牌橙文字；当前页使用 `brand-selected` 背景、
  `brand` 文字和透明边框。键盘焦点使用项目统一的 1px 品牌橙焦点环，禁用状态
  透明度为 50%。
- 完整分页默认显示总记录数、每页条数、上一页、页码、下一页与跳页输入；每页条数
  默认提供 10、20、50 三档，切换后必须回到第 1 页。跳页值必须限制在有效页码范围内。
- 桌面端左侧放置总记录数和每页条数，右侧放置页码导航和跳页；窄屏上下堆叠，
  上一页和下一页隐藏文字，仅保留图标，页码只保留首页、当前页和末页，且不得造成
  页面整体横向滚动。
- 通过 `<Table footer={<DataPagination ... />}>` 将分页放入同一数据区域，位于
  表头与表体的独立外框下方；分页自身不另加外框或阴影，不得以包含分页的外框
  替代表格区域的完整外框。分页与表格底边紧邻时共用一条分隔线，有间距时可按需
  绘制顶部 1px 分隔线，避免相邻边框重叠。

## 组合与无障碍

- 优先组合现有 shadcn 组件，不重复制作已有基础控件。
- 表单控件必须有可访问的标签、说明和校验状态。
- Dialog、Sheet 和 Drawer 必须提供可访问标题。
- Avatar 必须提供 fallback。
- 加载、空状态、提示、分隔线、标签和 toast 使用对应项目组件。
- 所有交互必须支持键盘操作，并使用品牌橙 `ring` token 保持可见的
  焦点状态；错误态继续使用 `destructive` 焦点反馈。

## 变更流程

1. 运行 `pnpm shadcn info --json` 确认项目配置。
2. 使用 `pnpm shadcn search` 或官方文档查找组件。
3. 使用 `pnpm shadcn add <name> --dry-run` 预览变更。
4. 安装或更新项目内组件。
5. 检查 `src/components/ui` 和 `src/app/globals.css` 的差异。
6. 运行 `pnpm lint` 和 `pnpm build`。

## 禁止事项

- 不在 `~/.codex/AGENTS.md` 或其他全局 AI 指令中加入本规范。
- 不在 `~/.agents/skills` 或 `~/.codex/skills` 安装本项目 Skill。
- 不复制其他项目的 `components.json`、`globals.css` 或 `components/ui`。
- 不引用目标项目之外的 UI 源码。
